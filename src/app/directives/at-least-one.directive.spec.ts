import { AtLeastOneDirective } from './at-least-one.directive';

describe('AtLeastOneDirective', () => {
  it('should create an instance', () => {
    const directive = new AtLeastOneDirective();
    expect(directive).toBeTruthy();
  });
});
